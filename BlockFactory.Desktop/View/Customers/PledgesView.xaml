<UserControl
    x:Class="BlockFactory.Desktop.Views.Customers.PledgesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    FlowDirection="RightToLeft"
    Background="{StaticResource ErpBrushPageBg}"
    Loaded="UserControl_Loaded">

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- إحصائيات -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,12">
            <Border Background="{StaticResource ErpBrushCard}" CornerRadius="12" Padding="16,12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="رهون نشطة" FontSize="12" Foreground="{StaticResource ErpBrushTextSecondary}"/>
                    <TextBlock Text="{Binding ActivePledges}" FontSize="22" FontWeight="Bold"
                               Foreground="#27AE60"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource ErpBrushCard}" CornerRadius="12" Padding="16,12" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="متأخرة" FontSize="12" Foreground="{StaticResource ErpBrushTextSecondary}"/>
                    <TextBlock Text="{Binding OverduePledges}" FontSize="22" FontWeight="Bold"
                               Foreground="#E74C3C"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource ErpBrushCard}" CornerRadius="12" Padding="16,12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="قريبة الاستحقاق" FontSize="12" Foreground="{StaticResource ErpBrushTextSecondary}"/>
                    <TextBlock Text="{Binding DueSoonPledges}" FontSize="22" FontWeight="Bold"
                               Foreground="#F39C12"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- شريط أدوات -->
        <Border Grid.Row="1" Background="{StaticResource ErpBrushCard}" CornerRadius="12" Padding="16,12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="الفلتر:" VerticalAlignment="Center" Margin="0,0,8,0"
                               FontSize="13"/>
                    <ComboBox ItemsSource="{Binding FilterOptions}"
                              SelectedItem="{Binding SelectedFilter}"
                              Width="160" Height="36" FontSize="13"/>
                </StackPanel>
                <Button Grid.Column="2" Content="استرجاع الرهن المحدد"
                        Command="{Binding ReturnPledgeCommand}"
                        Height="36" Padding="16,0"
                        Background="#E67E22" Foreground="White"
                        BorderThickness="0" FontSize="13" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- الجدول -->
        <Border Grid.Row="2" Background="{StaticResource ErpBrushCard}" CornerRadius="12" Padding="0">
            <DataGrid ItemsSource="{Binding Pledges}"
                      SelectedItem="{Binding SelectedPledge}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      FontSize="13"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="الهاتف" Binding="{Binding CustomerPhone}" Width="120"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding PledgeType}" Width="100"/>
                    <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="90"/>
                    <DataGridTextColumn Header="الاستحقاق" Binding="{Binding DueDateText}" Width="120"/>
                    <DataGridTextColumn Header="طلب مرتبط" Binding="{Binding RelatedOrderNumber}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
